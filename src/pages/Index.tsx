
import { Layout } from "@/components/layout/Layout";
import { Calendar } from "@/components/calendar/Calendar";

const Index = () => {
  return (
    <Layout>
      <h1 className="text-2xl font-bold mb-6">Family Planning Hub</h1>
      <Calendar />
    </Layout>
  );
};

export default Index;
